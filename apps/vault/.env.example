# ===========================================
# Nimble Vault External Content Configuration
# ===========================================

# External content repository configuration
# ------------------------------------------
# The vault content is fetched from an external repository at build time.
# This allows keeping sensitive RPG content in a private repository while
# the main application codebase remains public.

# Repository URL (can be local path or remote git URL)
# Local path example:
VAULT_CONTENT_REPO=/Users/six/prototype/nimble-vault

# Remote repository examples:
# VAULT_CONTENT_REPO=git@github.com:your-org/nimble-vault-content.git
# VAULT_CONTENT_REPO=https://github.com/your-org/nimble-vault-content.git

# Branch to fetch from (defaults to 'main')
VAULT_CONTENT_BRANCH=main

# Expected content structure:
# The external repository should contain:
# └── vault-content/
#     └── content/
#         └── docs/
#             ├── Heroes/
#             ├── Magic/
#             ├── Items/
#             ├── Foes/
#             ├── System/
#             └── Homebrew (Optional)/

# Notes:
# ------
# 1. Content is automatically fetched before dev and build commands
# 2. Fetched content is excluded from git via .gitignore
# 3. If fetch fails, placeholder content is generated
# 4. For production deployments, ensure the external repository is accessible